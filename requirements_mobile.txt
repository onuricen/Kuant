# Mobile GPU optimized requirements for GTX 1060 Ti and similar
# Tested for 6GB VRAM constraints

torch>=1.12.0,<2.0.0  # Stable version for mobile GPUs
torchvision>=0.13.0,<0.15.0
pytorch-forecasting>=0.10.0  # Stable TFT implementation
pytorch-lightning>=1.8.0,<2.0.0  # Compatible with pytorch-forecasting

# Core ML libraries
pandas>=1.5.0
numpy>=1.21.0,<1.24.0  # Avoid memory issues with newer versions
scikit-learn>=1.1.0,<1.3.0

# RL Framework
stable-baselines3>=1.6.0,<2.0.0
gymnasium>=0.26.0,<0.29.0

# Memory-efficient versions
psutil>=5.9.0  # Memory monitoring
joblib>=1.2.0

# Visualization (lightweight)
matplotlib>=3.5.0
seaborn>=0.11.0

# MLOps (optional)
mlflow>=2.0.0

# Development utilities
python-dotenv>=0.19.0
pathlib2>=2.3.0  # Compatibility

# CUDA-compatible versions for GTX 1060 Ti
# For CUDA 11.x support (adjust based on your driver)
# Uncomment if you need specific CUDA versions:
# torch==1.13.1+cu117
# torchvision==0.14.1+cu117
# --extra-index-url https://download.pytorch.org/whl/cu117 